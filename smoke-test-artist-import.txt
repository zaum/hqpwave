Smoke test: artist import and image endpoint
Date: 2026-03-20

Actions performed:
- Started server (node server/server).
- POST /endpoints/artistImport?name=Amon%20Tobin (imported artist data).
- GET /endpoints/artist?name=Amon%20Tobin (fetched stored artist JSON).
- GET /endpoints/artistImage for a release image id (observed 302 redirect to CoverArtArchive).
- GET /endpoints/artistImage for wiki image id (observed 302 redirect to Wikimedia).
- GET /endpoints/artistImage for nonexistent id (observed 404 JSON {"error":"image_not_found"}).

Observed results:
- Artist JSON returned with `images` and `discography` fields; default image id set.
- Release and wiki image requests correctly redirect to their remote URLs.
- Unknown image ids correctly return 404 with JSON error.

Commands used (for reproducibility):
curl -X POST "http://localhost:8000/endpoints/artistImport?name=Amon%20Tobin"
curl "http://localhost:8000/endpoints/artist?name=Amon%20Tobin"
curl -i "http://localhost:8000/endpoints/artistImage?artist_id=<id>&image_id=<image-id>"

Notes:
- `server/artist-handler.js` was patched to support synthetic release image ids by redirecting to `release.cover_url` when present.
- Further verification (UI render checks) can be done by opening the app in a browser and viewing the artist discography.

Log generated by GitHub Copilot assistant.